import { describe, it } from "vitest";

describe("authService", () => {
  it.todo("registers a user");
  it.todo("logs in a user");
  it.todo("refreshes tokens");
  it.todo("rejects duplicate email on register");
  it.todo("rejects invalid credentials on login");
});

