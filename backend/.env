# SQLite dev database
DATABASE_URL="file:./dev.db"

# JWT secrets (just random strings for dev)
JWT_ACCESS_SECRET="mydevaccesssecret"
JWT_REFRESH_SECRET="mydevrefreshsecret"

# Use mock scanner for now (no Docker)
USE_MOCK_SCANNER=true
